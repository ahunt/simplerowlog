simple rowLog TODO file.

-- Implement Database: (remember to put prepared statements locally.)
	[Done] addBoat(), getBoat(), modifyBoat(), getBoats(), getBoats(boolean)
			addMember(...), getMember(int), getMembers(), addGroup(...),
			getGroup(int), modifyGroup(...), getGroups(), modifyMember(...)

-- Update the Database documentation to do the same type of checking.
	[COMP] --> Now do the implementation
	
-- Update the error system, in the gui do the error catching and create an
	appropriate user friendly warning. Log all error at database level though.
	
	--checking for no member, no group etc.

-- In the database, for all getGroups etc, extract data directly from one request
	(instead of simply getting ids, then single groups).
	
-- Cache and cache checking in db, and in things like OutingDialog to check for
	last update.
	
-- Error cleaning on opening of database, closing of db.